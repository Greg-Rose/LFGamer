<h1>Games</h1>

<% @games.each do |game| %>
  <%= image_tag(game.cover_image.url) %>
  <%= game.name %>
  <% game.consoles.each do |c| %>
    <% if c.abbreviation %>
      <%= c.abbreviation %>
    <% else %>
      <%= c.name %>
    <% end %>
  <% end %>
<% end %>
